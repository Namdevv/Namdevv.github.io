# assets

Put your portrait photo here as **`profile.jpg`** (square works best, e.g. 400×400 or larger).

```
assets/profile.jpg
```

The hero will show it automatically inside the gradient ring.
If the file is missing, the site falls back to the initials "NT".
